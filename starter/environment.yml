name: fastapi  # Change this to your desired environment name
channels:
  - conda-forge
dependencies:
  - python=3.8.18  # Specific patch version for compatibility
  - pytest
  - pytest-cov
  - fastapi
  - uvicorn
  - flake8
  - pip
  - numpy
  - pandas
  - scikit-learn
  - joblib